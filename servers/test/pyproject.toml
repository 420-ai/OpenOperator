[project]
name = "test-servers"
version = "0.1.0"
description = "Project to test the servers for OO"
readme = "README.md"
authors = [
    { name = "Lukas Kellerstein", email = "lukas.kellerstein@microsoft.com" }
]
requires-python = ">=3.12"
dependencies = [
    "python-dotenv>=1.0.1",
    "requests>=2.32.3",
    "pillow>=10.3.0",
    "mcp>=1.6.0",
]
