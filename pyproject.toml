[project]
name = "echo_operator"
version = "0.1.0"
description = "The Echo Operator workspace"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[tool.uv.workspace]
members = ["models/ui", "agents/*", ]