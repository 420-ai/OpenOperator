# Computers

This folder contains setup for computers that are operated via OpenOperator. Based on: https://github.com/dockur/macos

# 1. MacOS

In folder `macos/docker` is setup for a VM in docker.

### 1.1 RDP

You can open the VM via `http://localhost:8007`
