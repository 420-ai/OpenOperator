# Docker

Build

```bash
docker build --no-cache --progress=plain -t lukaskellerstein/windows-computer:0.0.1 .
```

Push

```bash
docker push lukaskellerstein/windows-computer:0.0.1
```

[Docker Hub tags](https://hub.docker.com/repository/docker/lukaskellerstein/windows-computer/tags)
