[project]
name = "agent-oo4"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
authors = [
    { name = "Lukas Kellerstein", email = "lukas.kellerstein@microsoft.com" }
]
dependencies = [
    "core",
    "functions",
    "asyncio>=3.4.3",
    "gymnasium>=1.1.1",
    "python-dotenv>=1.0.1",
    "pillow>=11.1.0",
]

[tool.uv.sources]
core = { path = "../core" }
functions = { path = "../functions" }
